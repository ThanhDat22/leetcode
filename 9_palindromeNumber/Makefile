# Makefile for Palindrome Number Checker

CXX = g++
CXXFLAGS = -g -Wall -Wextra

TARGET = palindrome
SRC = Palindrome_Number.cpp

all: $(TARGET)

$(TARGET): $(SRC)
	$(CXX) $(CXXFLAGS) -o $(TARGET) $(SRC)

clean:
	rm -f $(TARGET)
